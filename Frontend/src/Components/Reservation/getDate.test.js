import getDate from './getDate';

describe('getDate', () => {
  it('should expose a function', () => {
		expect(getDate).toBeDefined();
	});
  
  it('getDate should return expected output', () => {
    // const retValue = getDate(date);
    expect(false).toBeTruthy();
  });
});
import minTimeWarning from './minTimeWarning';

describe('minTimeWarning', () => {
  it('should expose a function', () => {
		expect(minTimeWarning).toBeDefined();
	});
  
  it('minTimeWarning should return expected output', () => {
    // const retValue = minTimeWarning(setWarningMessage,setShowWarning);
    expect(false).toBeTruthy();
  });
});
import minDateAttribute from './minDateAttribute';

describe('minDateAttribute', () => {
  it('should expose a function', () => {
		expect(minDateAttribute).toBeDefined();
	});
  
  it('minDateAttribute should return expected output', () => {
    // const retValue = minDateAttribute();
    expect(false).toBeTruthy();
  });
});
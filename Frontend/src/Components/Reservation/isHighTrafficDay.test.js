import isHighTrafficDay from './isHighTrafficDay';

describe('isHighTrafficDay', () => {
  it('should expose a function', () => {
		expect(isHighTrafficDay).toBeDefined();
	});
  
  it('isHighTrafficDay should return expected output', () => {
    // const retValue = isHighTrafficDay();
    expect(false).toBeTruthy();
  });
});